---
title: 设计模式
category:
  - 面试题
tag:
  - java
date:
  - 2025-11-12
---

# Java 面试题 - 设计模式详解

设计模式是软件开发中经过验证的、可复用的解决方案。本文将详细讲解7种常见的设计模式,包括概念、代码示例和实际应用场景。

---

## 1. 单例模式 (Singleton Pattern)

### 概念

确保一个类只有一个实例,并提供全局访问点。

### 应用场景
- 数据库连接池
- 线程池
- 配置管理器
- 日志记录器

### 饿汉式(线程安全)

```java
public class Singleton {
    // 类加载时就创建实例
    private static final Singleton INSTANCE = new Singleton();
    
    private Singleton() {}
    
    public static Singleton getInstance() {
        return INSTANCE;
    }
}
```

#### 为什么是线程安全的

这是**饿汉式单例**，线程安全的根本原因在于：**JVM 的类加载机制天然保证了线程安全**。

---

#### 类初始化的完整过程

<img :src="$withBase('/assets/images/interview/ehs.png')" 
  alt="饿汉式"
  width="800px" 
  height="auto">

**核心原因：类初始化由 JVM 保证**

`private static final Singleton INSTANCE = new Singleton();` 这行代码在**类初始化阶段**执行，而不是在运行时某个方法里执行。

JVM 对类初始化有严格规定（Java 语言规范 §12.4.2）：

**① 初始化互斥**：JVM 内部为每个类维护一把初始化锁（`Class` 对象级别的锁）。当第一个线程触发类加载时，会获取这把锁；其他线程进来后发现"正在初始化"，会直接挂起等待。

**② 只执行一次**：初始化完成后，JVM 会更新状态为"已完成"，后续所有线程判断状态后直接跳过初始化，拿到同一个 `INSTANCE`。

**③ 可见性保证**：初始化完成后，`INSTANCE` 的写入对所有线程可见（`final` 字段的内存语义 + happens-before 关系保证）。

---

#### 与懒汉式对比

```java
// ❌ 懒汉式（不安全）——初始化在普通方法里，JVM 不管
public static Singleton getInstance() {
    if (instance == null) {          // 线程 A、B 同时通过检查
        instance = new Singleton();  // 可能创建两个实例
    }
    return instance;
}
```

饿汉式把"创建实例"这件事交给了 JVM 的类加载机制，从根本上绕开了并发问题——JVM 替你加了锁，你根本不需要写任何同步代码。

代价是：**类加载时就创建实例，即使你从未调用 `getInstance()`**，实例也已经存在了。

### 懒汉式-双重检查锁(DCL)
```java
public class Singleton {
    private volatile static Singleton instance;
    
    private Singleton() {}
    
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) {
                    instance = new Singleton();
                }
            }
        }
        return instance;
    }
}
```

### 枚举方式(最佳实践)

```java
public enum Singleton {
    INSTANCE;
    
    public void doSomething() {
        System.out.println("执行业务逻辑");
    }
}

// 使用
Singleton.INSTANCE.doSomething();
```

枚举单例被认为是最佳实践，主要是因为它**堵住了其他写法的两个漏洞**。

其他写法的两个致命问题

**漏洞 1：反射攻击**

```java
// 针对普通饿汉式/懒汉式，可以这样破坏单例
Constructor<Singleton> c = Singleton.class.getDeclaredConstructor();
c.setAccessible(true); // 关闭访问检查，强行解锁私有权限
Singleton s2 = c.newInstance(); // 成功创建第二个实例！
```

枚举天然防御：JVM 在 `newInstance()` 时会检查是否是枚举类型，直接抛 `IllegalArgumentException`，**无法绕过**。

**漏洞 2：反序列化攻击**

```java
// 普通单例序列化后再反序列化，会产生新实例
ObjectOutputStream oos = new ObjectOutputStream(...);
oos.writeObject(instance);
// 反序列化回来的是一个全新对象，不是同一个实例
```

枚举天然防御：枚举的反序列化由 JVM 特殊处理，**永远通过枚举名找回原实例**，不会创建新对象。

---

**对比总结**

| | 饿汉式 | 双重检查锁 | 静态内部类 | 枚举 |
|--|--|--|--|--|
| 线程安全 | ✅ | ✅ | ✅ | ✅ |
| 懒加载 | ❌ | ✅ | ✅ | ❌ |
| 防反射 | ❌ | ❌ | ❌ | ✅ |
| 防反序列化 | ❌ | ❌ | ❌ | ✅ |
| 代码简洁 | 一般 | 复杂 | 一般 | ✅ |

---

### 唯一的小缺点

枚举和饿汉式一样，**不支持懒加载**——`Singleton` 类被加载时 `INSTANCE` 就创建了。

绝大多数场景下这不是问题。但如果单例初始化非常重（比如要连数据库、读大文件），又确实需要懒加载，才需要考虑静态内部类的写法。

> 《Effective Java》第 3 条明确推荐枚举单例，Josh Bloch 原话是：单元素枚举是实现单例的最佳方式。


### 框架应用
- **Spring**: ApplicationContext、Bean(默认单例作用域)
- **MyBatis**: SqlSessionFactory
- **Runtime类**: `Runtime.getRuntime()`

---

## 2. 工厂模式 (Factory Pattern)

### 概念
定义创建对象的接口,让子类决定实例化哪个类,将对象的创建与使用分离。

### 应用场景
- 对象创建逻辑复杂
- 需要根据不同条件创建不同对象
- 隐藏对象创建细节

### 代码示例

**简单工厂模式**
```java
// 产品接口
interface Product {
    void use();
}

// 具体产品
class ConcreteProductA implements Product {
    @Override
    public void use() {
        System.out.println("使用产品A");
    }
}

class ConcreteProductB implements Product {
    @Override
    public void use() {
        System.out.println("使用产品B");
    }
}

// 简单工厂
class SimpleFactory {
    public static Product createProduct(String type) {
        switch (type) {
            case "A":
                return new ConcreteProductA();
            case "B":
                return new ConcreteProductB();
            default:
                throw new IllegalArgumentException("未知产品类型");
        }
    }
}

// 使用
Product product = SimpleFactory.createProduct("A");
product.use();
```

**工厂方法模式**
```java
// 抽象工厂
interface Factory {
    Product createProduct();
}

// 具体工厂
class FactoryA implements Factory {
    @Override
    public Product createProduct() {
        return new ConcreteProductA();
    }
}

class FactoryB implements Factory {
    @Override
    public Product createProduct() {
        return new ConcreteProductB();
    }
}

// 使用
Factory factory = new FactoryA();
Product product = factory.createProduct();
product.use();
```

### 框架应用
- **Spring**: BeanFactory、ApplicationContext
- **JDBC**: `DriverManager.getConnection()`
- **日志框架**: `LoggerFactory.getLogger()`

---

## 3. 观察者模式 (Observer Pattern)

### 概念
定义对象间一对多的依赖关系,当一个对象状态改变时,所有依赖它的对象都会收到通知并自动更新。

### 应用场景
- 事件监听机制
- 消息订阅/发布
- MVC架构中Model变化通知View

### 代码示例

```java
import java.util.ArrayList;
import java.util.List;

// 观察者接口
interface Observer {
    void update(String message);
}

// 主题(被观察者)
class Subject {
    private List<Observer> observers = new ArrayList<>();
    private String state;
    
    public void attach(Observer observer) {
        observers.add(observer);
    }
    
    public void detach(Observer observer) {
        observers.remove(observer);
    }
    
    public void setState(String state) {
        this.state = state;
        notifyAllObservers();
    }
    
    private void notifyAllObservers() {
        for (Observer observer : observers) {
            observer.update(state);
        }
    }
}

// 具体观察者
class ConcreteObserver implements Observer {
    private String name;
    
    public ConcreteObserver(String name) {
        this.name = name;
    }
    
    @Override
    public void update(String message) {
        System.out.println(name + " 收到通知: " + message);
    }
}

// 使用示例
public class ObserverDemo {
    public static void main(String[] args) {
        Subject subject = new Subject();
        
        Observer observer1 = new ConcreteObserver("观察者1");
        Observer observer2 = new ConcreteObserver("观察者2");
        
        subject.attach(observer1);
        subject.attach(observer2);
        
        subject.setState("状态已更新!");
    }
}
```

### 框架应用
- **Spring**: ApplicationEvent、ApplicationListener
- **Java**: `java.util.Observable`、`java.util.Observer`
- **GUI框架**: 按钮点击事件监听

---

## 4. 策略模式 (Strategy Pattern)

### 概念
定义一系列算法,将每个算法封装起来,使它们可以互相替换,让算法独立于使用它的客户端。

### 应用场景
- 多种支付方式(支付宝、微信、银行卡)
- 不同的排序算法
- 不同的压缩算法
- 促销活动中不同的折扣策略

### 代码示例

```java
// 策略接口
interface PaymentStrategy {
    void pay(int amount);
}

// 具体策略
class AlipayStrategy implements PaymentStrategy {
    private String account;
    
    public AlipayStrategy(String account) {
        this.account = account;
    }
    
    @Override
    public void pay(int amount) {
        System.out.println("使用支付宝支付 " + amount + " 元,账号: " + account);
    }
}

class WeChatPayStrategy implements PaymentStrategy {
    private String openId;
    
    public WeChatPayStrategy(String openId) {
        this.openId = openId;
    }
    
    @Override
    public void pay(int amount) {
        System.out.println("使用微信支付 " + amount + " 元,OpenId: " + openId);
    }
}

class CreditCardStrategy implements PaymentStrategy {
    private String cardNumber;
    
    public CreditCardStrategy(String cardNumber) {
        this.cardNumber = cardNumber;
    }
    
    @Override
    public void pay(int amount) {
        System.out.println("使用信用卡支付 " + amount + " 元,卡号: " + cardNumber);
    }
}

// 上下文
class ShoppingCart {
    private PaymentStrategy paymentStrategy;
    
    public void setPaymentStrategy(PaymentStrategy paymentStrategy) {
        this.paymentStrategy = paymentStrategy;
    }
    
    public void checkout(int amount) {
        paymentStrategy.pay(amount);
    }
}

// 使用示例
public class StrategyDemo {
    public static void main(String[] args) {
        ShoppingCart cart = new ShoppingCart();
        
        // 使用支付宝支付
        cart.setPaymentStrategy(new AlipayStrategy("user@alipay.com"));
        cart.checkout(100);
        
        // 切换为微信支付
        cart.setPaymentStrategy(new WeChatPayStrategy("wx123456"));
        cart.checkout(200);
    }
}
```

### 框架应用
- **Spring**: Resource接口的不同实现(ClassPathResource、FileSystemResource)
- **Java**: `Comparator`接口
- **ThreadPoolExecutor**: 不同的拒绝策略

---

## 5. 模板方法模式 (Template Method Pattern)

### 概念
定义算法骨架,将某些步骤延迟到子类实现,使子类可以在不改变算法结构的情况下重新定义某些步骤。

### 应用场景
- 数据库访问流程(连接、执行、关闭)
- 测试框架的setUp和tearDown
- 生命周期方法

### 代码示例

```java
// 抽象模板类
abstract class DataParser {
    // 模板方法 - 定义算法骨架
    public final void parseData(String path) {
        String data = readData(path);
        String parsedData = parseDataImpl(data);
        processData(parsedData);
        closeResource();
    }
    
    // 通用步骤
    private String readData(String path) {
        System.out.println("读取文件: " + path);
        return "raw data from " + path;
    }
    
    // 抽象方法 - 由子类实现
    protected abstract String parseDataImpl(String rawData);
    
    // 钩子方法 - 可选实现
    protected void processData(String data) {
        System.out.println("处理数据: " + data);
    }
    
    private void closeResource() {
        System.out.println("关闭资源");
    }
}

// 具体实现 - JSON解析器
class JsonParser extends DataParser {
    @Override
    protected String parseDataImpl(String rawData) {
        System.out.println("解析JSON格式数据");
        return "parsed json: " + rawData;
    }
}

// 具体实现 - XML解析器
class XmlParser extends DataParser {
    @Override
    protected String parseDataImpl(String rawData) {
        System.out.println("解析XML格式数据");
        return "parsed xml: " + rawData;
    }
    
    @Override
    protected void processData(String data) {
        System.out.println("XML特殊处理: " + data);
    }
}

// 使用示例
public class TemplateDemo {
    public static void main(String[] args) {
        DataParser jsonParser = new JsonParser();
        jsonParser.parseData("data.json");
        
        System.out.println("\n---\n");
        
        DataParser xmlParser = new XmlParser();
        xmlParser.parseData("data.xml");
    }
}
```

### 框架应用
- **Spring**: JdbcTemplate、RestTemplate
- **Servlet**: HttpServlet的doGet、doPost方法
- **JUnit**: @Before、@Test、@After执行流程

---

## 6. 装饰器模式 (Decorator Pattern)

### 概念
动态地给对象添加额外的职责,比继承更灵活地扩展功能。

### 应用场景
- IO流的包装(BufferedReader包装FileReader)
- 给组件添加边框、滚动条等功能
- 缓存、日志、权限控制等横切关注点

### 代码示例

```java
// 组件接口
interface Coffee {
    String getDescription();
    double getCost();
}

// 具体组件
class SimpleCoffee implements Coffee {
    @Override
    public String getDescription() {
        return "普通咖啡";
    }
    
    @Override
    public double getCost() {
        return 10.0;
    }
}

// 装饰器抽象类
abstract class CoffeeDecorator implements Coffee {
    protected Coffee decoratedCoffee;
    
    public CoffeeDecorator(Coffee coffee) {
        this.decoratedCoffee = coffee;
    }
    
    @Override
    public String getDescription() {
        return decoratedCoffee.getDescription();
    }
    
    @Override
    public double getCost() {
        return decoratedCoffee.getCost();
    }
}

// 具体装饰器 - 加牛奶
class MilkDecorator extends CoffeeDecorator {
    public MilkDecorator(Coffee coffee) {
        super(coffee);
    }
    
    @Override
    public String getDescription() {
        return decoratedCoffee.getDescription() + " + 牛奶";
    }
    
    @Override
    public double getCost() {
        return decoratedCoffee.getCost() + 2.0;
    }
}

// 具体装饰器 - 加糖
class SugarDecorator extends CoffeeDecorator {
    public SugarDecorator(Coffee coffee) {
        super(coffee);
    }
    
    @Override
    public String getDescription() {
        return decoratedCoffee.getDescription() + " + 糖";
    }
    
    @Override
    public double getCost() {
        return decoratedCoffee.getCost() + 1.0;
    }
}

// 使用示例
public class DecoratorDemo {
    public static void main(String[] args) {
        // 普通咖啡
        Coffee coffee = new SimpleCoffee();
        System.out.println(coffee.getDescription() + " 价格: " + coffee.getCost());
        
        // 加牛奶
        coffee = new MilkDecorator(coffee);
        System.out.println(coffee.getDescription() + " 价格: " + coffee.getCost());
        
        // 再加糖
        coffee = new SugarDecorator(coffee);
        System.out.println(coffee.getDescription() + " 价格: " + coffee.getCost());
    }
}
```

### 框架应用
- **Java IO**: BufferedReader、BufferedInputStream等包装流
- **Spring**: BeanWrapper
- **MyBatis**: Cache装饰器链

---

## 7. 代理模式 (Proxy Pattern)

### 概念
为其他对象提供一种代理以控制对这个对象的访问。

### 应用场景
- 远程代理(RPC)
- 虚拟代理(延迟加载)
- 保护代理(权限控制)
- AOP(面向切面编程)

### 代码示例

**静态代理**
```java
// 接口
interface UserService {
    void addUser(String username);
}

// 真实对象
class UserServiceImpl implements UserService {
    @Override
    public void addUser(String username) {
        System.out.println("添加用户: " + username);
    }
}

// 代理对象
class UserServiceProxy implements UserService {
    private UserService userService;
    
    public UserServiceProxy(UserService userService) {
        this.userService = userService;
    }
    
    @Override
    public void addUser(String username) {
        System.out.println("代理: 权限检查");
        System.out.println("代理: 日志记录 - 开始");
        
        userService.addUser(username);
        
        System.out.println("代理: 日志记录 - 结束");
    }
}
```

**JDK动态代理**
```java
import java.lang.reflect.InvocationHandler;
import java.lang.reflect.Method;
import java.lang.reflect.Proxy;

class DynamicProxyHandler implements InvocationHandler {
    private Object target;
    
    public DynamicProxyHandler(Object target) {
        this.target = target;
    }
    
    @Override
    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
        System.out.println("动态代理: 方法执行前 - " + method.getName());
        Object result = method.invoke(target, args);
        System.out.println("动态代理: 方法执行后");
        return result;
    }
    
    public Object getProxy() {
        return Proxy.newProxyInstance(
            target.getClass().getClassLoader(),
            target.getClass().getInterfaces(),
            this
        );
    }
}

// 使用示例
public class ProxyDemo {
    public static void main(String[] args) {
        // 静态代理
        UserService userService = new UserServiceImpl();
        UserService proxy = new UserServiceProxy(userService);
        proxy.addUser("张三");
        
        System.out.println("\n--- 动态代理 ---\n");
        
        // JDK动态代理
        UserService realService = new UserServiceImpl();
        DynamicProxyHandler handler = new DynamicProxyHandler(realService);
        UserService dynamicProxy = (UserService) handler.getProxy();
        dynamicProxy.addUser("李四");
    }
}
```

**CGLIB动态代理**
```java
import net.sf.cglib.proxy.Enhancer;
import net.sf.cglib.proxy.MethodInterceptor;
import net.sf.cglib.proxy.MethodProxy;
import java.lang.reflect.Method;

// 目标类(无需实现接口)
class ProductService {
    public void addProduct(String productName) {
        System.out.println("添加商品: " + productName);
    }
}

// CGLIB拦截器
class CglibInterceptor implements MethodInterceptor {
    @Override
    public Object intercept(Object obj, Method method, Object[] args, MethodProxy proxy) throws Throwable {
        System.out.println("CGLIB代理: 方法执行前");
        Object result = proxy.invokeSuper(obj, args);
        System.out.println("CGLIB代理: 方法执行后");
        return result;
    }
}

// 使用
Enhancer enhancer = new Enhancer();
enhancer.setSuperclass(ProductService.class);
enhancer.setCallback(new CglibInterceptor());
ProductService proxy = (ProductService) enhancer.create();
proxy.addProduct("手机");
```

### 框架应用
- **Spring AOP**: 核心实现就是代理模式
  - 有接口时使用JDK动态代理
  - 无接口时使用CGLIB代理
  - 实现事务管理、日志、权限控制等
- **MyBatis**: Mapper接口的动态代理
- **RPC框架**: Dubbo、Feign的远程调用代理

---

## 设计模式对比总结

| 模式 | 目的 | 关键特征 | 典型应用 |
|-----|------|---------|---------|
| 单例 | 控制实例数量 | 全局唯一实例 | 配置管理、连接池 |
| 工厂 | 封装对象创建 | 创建与使用分离 | Spring Bean创建 |
| 观察者 | 对象间通信 | 一对多依赖 | 事件监听机制 |
| 策略 | 算法可替换 | 行为封装 | 支付方式选择 |
| 模板 | 定义算法骨架 | 子类实现细节 | JdbcTemplate |
| 装饰器 | 动态扩展功能 | 组合优于继承 | IO流包装 |
| 代理 | 控制访问 | 增强功能 | Spring AOP |

---

## 面试高频问题

### 1. 单例模式的几种实现方式?线程安全吗?
- 饿汉式(线程安全)、懒汉式(需要同步)、双重检查锁、静态内部类、枚举(推荐)

### 2. 代理模式的几种实现?区别是什么?
- 静态代理、JDK动态代理(需要接口)、CGLIB动态代理(继承类)
- Spring AOP默认使用JDK动态代理,可配置使用CGLIB

### 3. 装饰器模式和代理模式的区别?
- 装饰器:增强功能,可多层嵌套
- 代理:控制访问,一般一层

### 4. 工厂模式和抽象工厂模式的区别?
- 工厂方法:创建一个产品
- 抽象工厂:创建一系列相关产品

### 5. Spring中用到了哪些设计模式?
- 单例模式(Bean默认单例)
- 工厂模式(BeanFactory)
- 代理模式(AOP)
- 模板方法(JdbcTemplate)
- 观察者模式(事件监听)
- 适配器模式(HandlerAdapter)
- 策略模式(Resource)

---

## 设计模式参考资料

- https://tuonioooo-notebook.gitbook.io/application-framework/she-ji-mo-shi-pian
- https://coding.dzspace.top/article/design-mode-1.html
- https://coding.dzspace.top/article/design-mode-2.html

--

掌握这些设计模式不仅能帮助你写出更优雅的代码,也是通过Java面试的必备技能。建议结合实际项目多加练习,理解每种模式的适用场景和权衡取舍。

